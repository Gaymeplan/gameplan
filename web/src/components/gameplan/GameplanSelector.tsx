import { H1 } from '@blueprintjs/core';
import React from 'react';

type GameplanSelectorProps = {};

const GameplanSelector = (props: GameplanSelectorProps) => {
    return (
        <div>
            <H1>Gameplan Selector</H1>
        </div>
    );
};

export default GameplanSelector;
